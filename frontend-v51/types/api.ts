/**
 * DMS API Types — générés depuis OpenAPI (INV-F01).
 *
 * Local : `npm run gen:types` (API sur localhost:8000).
 * CI : `python scripts/export_openapi_main_app.py` puis `npm run gen:types:snapshot`
 * (voir `.github/workflows/dms_invariants_v51.yml`).
 * Ne pas laisser ce fichier en stub vide : le check CI `check_openapi_types_non_stub.py` échoue.
 *
 * @generated openapi-typescript
 */

export interface paths {}
export interface components {}
export interface operations {}
