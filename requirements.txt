fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
email-validator==2.1.0.post1

python-multipart==0.0.12
pandas==3.0.1
openpyxl==3.1.5
weasyprint>=60.0
Jinja2>=3.1.0
babel>=2.12.0
python-docx==1.1.2
pypdf==5.1.0
pdfplumber==0.11.4
python-dotenv==1.0.1
sqlalchemy==2.0.25
alembic==1.13.1
psycopg[binary,pool]==3.2.5
asyncpg>=0.29.0

# === SECURITY (M4A-F) ===
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
cryptography>=42.0.0
python-jose[cryptography]==3.3.0
slowapi==0.1.9
redis==5.2.1
filetype==1.2.0
werkzeug==3.1.3

pytest>=8.0.0
pytest-cov>=4.1.0
httpx==0.27.0
requests>=2.31.0

# LLM — annotation-backend + Tier-1 OCR (ADR-M11-002)
# mistral-large-latest + mistral-ocr-latest via client.ocr.process()
# 2.x : OCR document = { "type": "file", "file_id": ... } (voir engine._extract_mistral_ocr)
mistralai>=2.0.0,<3

# === RESILIENCE (M4D) ===
tenacity==9.0.0
pybreaker==1.2.0

# === M12 — Procurement Document & Process Recognizer ===
rapidfuzz>=3.6.0
pyyaml>=6.0

# === OCR / SSL resilience (cloud-first) ===
# certifi : bundle CA Mozilla pour contourner les proxies d'entreprise sur les appels Mistral OCR
certifi>=2024.2.2

# === VIVANT V2 — H2 Background workers (ADR-H2-ARQ-001) ===
arq==0.26.1

# === VIVANT V2 — H3 LLM Observability (ADR-H3-LANGFUSE-001) ===
langfuse>=2.0.0

# === VIVANT V2 — H3 Local Embeddings BGE-M3 (ADR-H3-BGE-M3-001) ===
# FlagEmbedding: local BGE-M3 + BGE-reranker-v2-m3, 0 external API calls
# Install only on machines with 8 GB+ RAM (Railway prod tier)
# In CI/tests, stubs are used automatically when FlagEmbedding is not importable
FlagEmbedding>=1.2.5

# === VIVANT V2 — H3 RAG Evaluation (ADR-RAGAS-001) ===
ragas>=0.1.0
