# Rapports d’audit (générés / hors gel)

Les fichiers `RAPPORT_AUDIT_*.md` sont des **artefacts d’audit** — le contenu détaillé peut être régénéré ou restauré depuis **OneDrive → Version précédente** si un nettoyage local les a supprimés.

Ne pas confondre avec `docs/freeze/` (référentiel gelé).
