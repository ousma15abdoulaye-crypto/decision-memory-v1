# Rapport — synchronisation référentiel vendors

**Statut :** *squelette régénéré*.

## Restauration

- OneDrive : version précédente.
- Comptages : `scripts/run_pg_sql.py -c "SELECT COUNT(*) FROM vendors;"` (avec `RAILWAY_DATABASE_URL` / `.env.railway.local`).

## Contenu attendu

- Environnement (Railway / local)
- Volume avant / après
- Anomalies
