# Shell scripts: force LF so Docker/Linux and Windows worktrees behave the same at build time
*.sh text eol=lf

# Freeze: always LF so sha256sum matches on Linux (CI)
docs/freeze/** text eol=lf

docs/freeze/**/*.md text eol=lf
docs/freeze/**/SHA256SUMS.txt text eol=lf
