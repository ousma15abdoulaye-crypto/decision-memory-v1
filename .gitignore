# ================================================================
# ARTEFACTS DE SESSION — JAMAIS VERSIONNER
# Ref : ASAP-01 — audit CTO senior 2026-03-17
# ================================================================

# Logs et sorties de scripts
alembic_m*.txt
audit_*.txt
backfill_*.txt
build_err.txt
build_out.txt
m11_*.txt
m12_*.txt
pytest_result*.txt
run_dict*.txt
phase_*.txt
probe_*.txt
*.log

# Outputs de scripts de session
scripts/_dryrun*.txt
scripts/_err*.txt
scripts/_out*.txt
scripts/_wave*.txt
scripts/_probe_*.py

# Runs orchestrateur annotation (JSON local)
data/annotation_pipeline_runs/

# Données de session potentiellement sensibles
mercurials_mapping_proposals.csv
*_mapping_proposals.csv
*_dry*.csv
*_output*.csv

# Frontend V5.1 — snapshot OpenAPI généré (CI / export local)
frontend-v51/openapi-snapshot.json

# Frontend M16 (Vite / Vitest)
frontend/node_modules/
frontend/dist/

# Artefacts Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
.venv/
env/
ENV/

# Environment / secrets (Constitution: no secrets in repo)
.env
.env.*
!.env.example
!.env.docker.example
!.env.railway.local.example
.env.local
.env.*.local

# IDEs
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# DMS specific
# Exports corpus M12 (R2 → JSONL local) — données réelles, ne pas versionner
data/annotations/m12_corpus*.jsonl
data/annotations/**/m12_corpus*.jsonl
# Manifeste local des lignes (peut contenir task_id / métadonnées)
data/annotations/m12_authoritative*_manifest.tsv
data/annotations/m12_corpus_authoritative_manifest.tsv
# Tout inventaire / manifeste TSV sous annotations (ex. m12_corpus_from_ls_manifest.tsv)
data/annotations/**/*manifest*.tsv
# Espace de travail local (env d’export, splits, copies — ne pas versionner)
data/annotations/annotations/
# Corpus réglementaire / PDF / extractions — travail local uniquement
data/regulatory/
# Sauvegardes M12 (script m12_corpus_backup.ps1)
data/annotations/backups/
# Fichiers autosave locaux (ls_local_autosave.py + DualCorpusSink backup)
data/annotations/ls_autosave*.jsonl
data/annotations/ls_backup*.jsonl
# Identifiants API R2 / S3 (fichiers locaux — jamais committer)
data/annotations/**/.r2_export_env
data/annotations/**/r2_export.env
# data/ ignoré sélectivement — .gitkeep et README.md restent trackés
data/**/*.xlsx
data/**/*.xls
data/**/*.csv
data/**/*.json
# Manifest figé OCR bridge (reconstruit depuis le freeze MD — pas de secrets)
!data/ingest/test_mistral_output/skipped.json
# Corpus M12 — JSONL volumineux hors git ; manifest seul versionné (mandat clôture)
!data/annotations/corpus_manifest.json
data/annotations/*.jsonl
data/annotations/corpus_m12_backup_*.jsonl
data/embeddings/
data/**/*.zip
# Cache LlamaCloud/extraction — artefacts générés, ne pas versionner
data/imports/m5/cache/
data/imports/imc/
# Corpus annotation — PDFs lourds / sensibles : dépôt local uniquement (comme m4 xlsx)
data/imports/annotation/**/*.pdf
data/ingest/test_mistral/**/*.pdf
*.sqlite3
*.db
uploads/
outputs/

# OS
.DS_Store
Thumbs.db

# Backup files
*.backup.xlsx
*.bak

# Dumps SQL de production — données sensibles, ne jamais versionner (Constitution: no secrets in repo)
*.sql
*_backup_*.sql
dms_prod_backup_*.sql
# Dry-run Alembic offline documenté (schéma uniquement, pas de dump prod)
!docs/ops/ALEMBIC_DRYRUN_090_to_092.sql
!docs/ops/ALEMBIC_DRYRUN_090_to_093.sql
# Scripts sécurité multi-tenant (mandat — pas de dump données)
!scripts/security/*.sql

# Logs
*.log
logs/

# Coverage reports
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.pytest_cache/

# Alembic cache (évite révisions fantômes)
alembic/__pycache__/
alembic/versions/__pycache__/

# dms.log explicite
dms.log

# P3.4 E4 benchmark artefacts (données sensibles — Option B)
rapports/p34_case_*.json

# Railway
.railway/

# CSV processés — optionnel selon décision CTO
# data/processed/

# Claude Code IDE artifacts (session-specific)
.claude/

# Cursor rules (except committed governance files)
.cursor/**
!.cursor/rules/dms-git-no-direct-main.mdc

# Local sandbox archives (CTO doctrine G7-G8 — archive don't delete)
local_sandbox_archive/

# Test data imports (ad-hoc session files)
data/imports/annotation/SUPPLIERS BUNDLE TEST*/
data/test_zip/
