# Inventaire M12 — emplacement réservé

Le contenu détaillé a été perdu lors d’un nettoyage local (`git clean`).

**Régénération :** exécuter l’inventaire sur votre export JSONL canonique et rediriger la sortie ici, par exemple :

```text
python scripts/inventory_m12_corpus_jsonl.py chemin/vers/export.jsonl > data/annotations/inventory_m12_latest.md
```

Voir aussi `README.md` dans ce dossier (récupération OneDrive).
